# Optional overrides. The launch scripts work without this file.
PHRASYNC_HOST=127.0.0.1
PHRASYNC_PORT=5500
PHRASYNC_MAX_UPLOAD_MB=2048

# Optional: set in your shell only if a gated Hugging Face model needs it.
# HF_TOKEN=your_personal_token
