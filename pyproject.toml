[project]
name = "phrasync"
version = "0.1.0"
description = "Local AI studio for multilingual lyric videos and subtitles with word-level timing, kinetic typography, and MP4 export."
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "jaydemks"}]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
